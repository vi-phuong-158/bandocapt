---
name: bandocapt-design
description: Use this skill to generate well-branded interfaces and assets for "Bản đồ Công an số" — the digital police-map & administrative-procedure assistant for Phú Thọ province (Vietnam) — either for production or throwaway prototypes/mocks. Contains design guidelines, colors, type, fonts, assets, and UI-kit components for prototyping.
user-invocable: true
---

Read the `README.md` file within this skill, and explore the other available files (tokens in `tokens/`, foundation cards in `guidelines/`, components in `components/`, the full UI kit in `ui_kits/`).

If creating visual artifacts (slides, mocks, throwaway prototypes, etc), copy assets out (`assets/logo.png`, `assets/icon.png`) and create static HTML files for the user to view, linking `styles.css` for tokens. If working on production code, copy assets and read the rules here to become an expert in designing with this brand.

If the user invokes this skill without any other guidance, ask them what they want to build or design, ask some questions, and act as an expert designer who outputs HTML artifacts _or_ production code, depending on the need.
